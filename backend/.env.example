# Google API Configuration
GOOGLE_API_KEY=your_google_api_key_here

# Model Configuration
MODEL_NAME=gemini-2.0-flash-exp
TEMPERATURE=0.7
MAX_TOKENS=2048

# Server Configuration
# SECURITY: Use 127.0.0.1 to bind to localhost only (recommended for development)
# Only use 0.0.0.0 if you need to expose the server to your network
HOST=127.0.0.1
PORT=8000
CORS_ORIGINS=["http://localhost:*","http://127.0.0.1:*"]

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=

# MCP Tools (Phase 2)
ENABLE_TOOLS=false
TOOL_REGISTRY_PATH=
